schema:
  schema_id: pdsp
  name: 拼读双拼
  version: 20260713
  author:
  description: 51声介，44韵调（+ong-eng）
  dependencies:

switches:
  - name: ascii_mode
    reset: 0
    states: [ 中文, 西文 ]
  - name: full_shape
    states: [ 半角, 全角 ]
  #- name: simplification
  #  states: [ 漢字, 汉字 ]
  - name: ascii_punct
    states: [ 。，, ．， ]

engine:
  processors:
    - ascii_composer
    - recognizer
    - lua_processor@*llm_processor
    - lua_processor@*key_extra_processor
    - lua_processor@*word_length_split_processor
    - key_binder
    - lua_processor@*pin_fix_processor
    - speller
    - punctuator
    - selector
    - navigator
    - express_editor
  segmentors:
    - ascii_segmentor
    - matcher
    - abc_segmentor
    - punct_segmentor
    - fallback_segmentor
  translators:
    - punct_translator
    - table_translator
  filters:
    - uniquifier
    - lua_filter@*hint_filter
    - lua_filter@*word_length_split_filter
    - lua_filter@*llm_filter
    - lua_filter@*pin_fix_filter
    - lua_filter@*no_match_placeholder
    #- simplifier

# LLM 候选重排配置（全部可选，省略则用 lua/cpp 内置默认值）
llm_rerank:
  min_code_len: 4
  max_tokens: 10
  max_candidates: 5
  min_tokens: 1    # 上文 token 数 < min_tokens 时不推理 (空 ctx 无推理是此配置的语义; 0 = 空上文也推理)
  backend: off       # cpu | gpu | off — 默认 off。用户需使用时改为 cpu/gpu
  # model_path: "d:/gguf_models/Qwen3.5-0.8B-Q4_K_M.gguf"  # 取消注释可指定模型路径，默认 Qwen3.5-0.8B Q4_K_M
  # cpu_cores: 7    # 取消注释可手动指定线程数，默认 7（实测多台机器均在 7 饱和）

speller:
  #编码唯一自动上屏
  auto_select: false
  #超最大码长重新编码
  #auto_clear: max_length
  max_code_length: 4
  alphabet: 'abcdefghijklmnopqrstuvwxyz'
  delimiter: " "
  algebra:

translator:
  dictionary: pdsp
  enable_charset_filter: false
  enable_completion: false
  enable_sentence: false
  enable_user_dict: true
  enable_encoder: true
  enable_commit_history: true
  max_phrase_length: 5
  disable_user_dict_for_patterns:
    - '^.{1,3}$'
  preedit_format:
    - xform/.*//
  comment_format:
    - xform/.*//

key_binder:
  bindings:
    #翻页，可选comma、period
    - { when: paging, accept: bracketleft, send: Page_Up }
    - { when: has_menu, accept: bracketright, send: Page_Down }
    - { when: has_menu, accept: Tab, send: Page_Down }
    #次选
    - { when: has_menu, accept: semicolon, send: 2 }
    #退格当清屏用
    - { when: composing, accept: BackSpace, send: Escape }

punctuator:
  import_preset: symbols_pd
  digit_separators: ""

recognizer:
  patterns:
    punct: '^\\([0-9][01]?|[A-Za-z]+)?$'
